/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // if get erorr make false ther e
  images: {
    domains: ['lh3.googleusercontent.com'],
  },
};

module.exports = nextConfig;
